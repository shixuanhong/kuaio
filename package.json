{
  "name": "@kuaio/kuaio-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^18.0.0 || >=20.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shixuanhong/kuaio.git"
  },
  "keywords": [],
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r --filter=./packages/* build",
    "publish:lib": "pnpm -r --filter=./packages/* publish:lib"
  },
  "devDependencies": {},
  "packageManager": "pnpm@8.0.0+"
}
